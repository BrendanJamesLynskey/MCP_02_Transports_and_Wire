# MCP Transports & The Wire

Every byte of MCP at the transport layer — stdio framing, the deprecated HTTP+SSE legacy, modern Streamable HTTP, session IDs, resumable streams via `Last-Event-ID`, auth headers, and a fully annotated wire capture of one real session.

**Live site:** https://brendanjameslynskey.github.io/MCP_02_Transports_and_Wire/

Part of the [Model Context Protocol series](https://github.com/BrendanJamesLynskey/LLMs#model-context-protocol-mcp).
